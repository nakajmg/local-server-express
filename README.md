# local-server-express package

Serve current project via Express.

## usage

`ctrl+alt+cmd+s` then serve current project and open browser.

or

open menu `cmd+shift+p` then choose `Local Server Express:run`
